# projetoIntegradorGrupo10
    install nodemon dev dependencies, install express for devoloper server, install template engine ejs.
    for init project local, is necessary initinit npm : npm init -y.